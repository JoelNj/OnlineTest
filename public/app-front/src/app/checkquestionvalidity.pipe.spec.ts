import { CheckquestionvalidityPipe } from './checkquestionvalidity.pipe';

describe('CheckquestionvalidityPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckquestionvalidityPipe();
    expect(pipe).toBeTruthy();
  });
});
